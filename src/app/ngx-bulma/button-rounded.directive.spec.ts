import { ButtonRoundedDirective } from './button-rounded.directive';

describe('ButtonRoundedDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonRoundedDirective();
    expect(directive).toBeTruthy();
  });
});
